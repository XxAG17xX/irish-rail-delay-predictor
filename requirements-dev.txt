# Development tooling. Installs requirements.txt as well, so this one file sets up a
# working environment:
#
#   pip install -r requirements-dev.txt
#
# Nothing here ships to Lambda, and nothing here is imported by src/. The one import is
# scripts/make_og_image.py, which redraws the link preview card and runs on a laptop. That
# is
# the whole reason for the split: cfn-lint alone accounts for six of these packages, and
# with them mixed into requirements.txt it looked as though the project used a computer
# algebra system.

-r requirements.txt

# Validates the CloudFormation templates before a deploy finds the problem.
cfn-lint==1.55.1

# cfn-lint's, all of them. sympy and mpmath are there because it solves Fn::If conditions
# symbolically; networkx because it graphs resource dependencies.
jsonpatch==1.33
jsonpointer==3.1.1
mpmath==1.3.0
networkx==3.6.1
PyYAML==6.0.3
regex==2026.7.19
sympy==1.14.0

# Draws site/og.png, the link preview card. Laptop only: the card is committed.
pillow==12.3.0
